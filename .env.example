# ===== 프론트엔드 빌드 시 사용 (Vite) =====
# GAS 웹 앱 배포 URL (배포 후 여기 넣으세요)
VITE_GAS_API_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec

# ===== Netlify 환경변수 (챗봇용) =====
# Netlify 대시보드 → Site settings → Environment variables 에서 반드시 설정하세요.
# 로컬에서 netlify dev 로 실행할 때만 .env 값이 함수에 전달됩니다.
OPENAI_API_KEY=sk-your-openai-api-key-here
